spring:
  application:
    name: lm

  datasource:
    url: jdbc:postgresql://localhost:5432/chatbot_db
    username: ernestocosio
    # password: your_password
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  

