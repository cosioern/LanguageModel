spring:
  application:
    name: lm

  datasource:
    url: jdbc:postgresql://localhost:5432/chatbot_db
    username: postrgesql
    password: your_password
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
  

